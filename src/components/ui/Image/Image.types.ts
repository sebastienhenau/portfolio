// -----------------------------------------------------------------------------
// Props
// -----------------------------------------------------------------------------
export type TImageSrc = string;
export type TImageAlt = string;

export interface TImage {
	src: TImageSrc;
	alt?: TImageAlt;
}
